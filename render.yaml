databases:
  - name: ledger-practice-db
    plan: free
    databaseName: ledger_practice
    user: ledger_practice

services:
  - type: web
    name: ledger-practice
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt && flask init-db && flask create-admin"
    startCommand: "gunicorn run:app"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: FLASK_APP
        value: run.py
      - key: DATABASE_URL
        fromDatabase:
          name: ledger-practice-db
          property: connectionString
      - key: ADMIN_NAME
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
